Concept Boolean_Template;

    Type Family Boolean is modeled by B;
        exemplar b;
        --initialization ensures b = true;

    Oper True (): Boolean;

    Oper False (): Boolean;

    Oper And (evaluates a, b: Boolean): Boolean;

    Oper Or (evaluates a, b: Boolean): Boolean;

end Boolean_Template;