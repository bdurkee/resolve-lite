package org.resolvelite.vcgen.applicationstrategies;

import org.resolvelite.parsing.ResolveParser;
import org.resolvelite.vcgen.applicationstrategies.RuleApplicationStrategy;
import org.resolvelite.vcgen.vcstat.AssertiveCode;
import org.resolvelite.vcgen.vcstat.VCVars;

public class SwapApplicationStrategy
        implements
            RuleApplicationStrategy<ResolveParser.SwapStmtContext> {

    @Override public void applyRule(ResolveParser.SwapStmtContext statement,
            AssertiveCode block) {

    }
}
