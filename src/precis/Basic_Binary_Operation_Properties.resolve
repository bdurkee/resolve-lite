Precis Basic_Binary_Operation_Properties;

    Definition Is_Associative(p : (D : SSet) * D ⟶ D) : 𝔹 is
        ∀ x, y, z : D, p(x, (p(y, z))) = p(p(x, y), z);
end Basic_Binary_Operation_Properties;